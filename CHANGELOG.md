# 更新说明

## v1.1.0 — 新增回装（pack）：改完素材重新封回 `.dat`

> 本项目从「只解包」变成「解包 + 回装」一体的工具：
> `th06nc-unpack` / `th06nc-pack`（同一个仓库、同一套逆向结论）。

### 新增

- **回装命令** `th06nc-pack`，解包工具本体也认 `th06nc-unpack pack …` 子命令。
  流程：解包 → 改素材 → 回装 → 按原名放回 `th06nc/data/`。
- **增量替换**：逐条比对内容，只替换真正改动的条目；未改动条目**连压缩字节都原样搬运**。
  把解包目录原样回装，产物与原 `.dat` **逐字节相同**（4 个归档实测）。
- **两种写法**：`rebuild`（默认，按官方布局紧凑重排）与 `--inplace`（追加式，
  除改动条目外一个字节都不动，改大的条目追加到文件末尾）。
- **可选 zstd 压缩**：默认 19 级 —— 实测与官方打包器同档，573 条压缩条目里
  449 条连字节都一致，帧头 100% 一致。压缩器按
  `Node 内置 zlib.zstdCompressSync`（Node ≥ 22.15 / 23.8）→ `@bokuweb/zstd-wasm`
  （可选依赖）→ 回退 raw 的顺序查找；**没有任何 zstd 依赖时也能正常回装**
  （官方归档里本来就有 375 条 raw，引擎两种都吃）。
- **产物校验** `--verify`：写完重开产物，逐条解码并与改动文件逐字节比对。
- **DDS 守卫**：改了形状（宽高 / 压缩格式 / 贴图层数）会告警；并按 DDS 头算出
  「本该有多少字节」，数据截断也会告警 —— 这是改贴图最常见的翻车点。
- **`--dry-run`**：只报告会改哪些条目，不落盘。

### 逆向出的新结论（回装依据）

- 索引密钥来自**归档文件名**（`CRC32(归档名)` → SplitMix64），文件里不存它 ——
  所以产物必须叫回 `<归档名>.dat`，改名等于游戏解不开索引。
- 数据区按 **16 字节对齐**：每条偏移都是 16 的倍数，索引区之后补齐到 16 才放数据，
  文件末尾补齐到 16（几个归档都以 1 字节 `0x00` 收尾）。
- 官方打包器用 **zstd 19 级**（`single-segment` 帧、无校验和、无字典），
  条目密钥种子为**每条独立随机 32 位**。

### 验证（真实数据，4 个归档 / 614 条记录 / 2.2 GB 明文）

| 检查项 | th06MD | th06ST | th06CM | th06IN |
|---|---|---|---|---|
| 原样回装 = 与原 `.dat` 逐字节相同 | ✓ | ✓ | ✓ | ✓ |
| 改两条后未改动条目载荷逐字节不变 | 36/36 | 167/167 | 309/309 | 94/94 |
| 产物全文可解码 | 38/38 | 169/169 | 311/311 | 96/96 |
| 编码器与官方逐字节一致（zstd 条目） | — | 150/168 | 252/310 | 47/95 |

### 免安装产物

Release 里现在有两份单文件：`th06nc-unpack.cjs`（解包）与 `th06nc-pack.cjs`（回装），
平台原生可执行（Node SEA）只含解包。回装建议用 `.cjs`（需要 Node ≥ 18）。

### 已知限制

- 回装**只支持替换已有条目**，不支持新增 / 删除条目。
- 改 ANM / ECL / MSG 这类带内部偏移表的容器，改长度要自己同步修正内部表。
- 「游戏真的能起来」请务必在有游戏本体的机器上进游戏实测一次。

---

## v1.0.0 — 首个版本

- 完整还原 TH06NC「PKGL」归档格式（7 个归档 / 1783 条记录 / 6.66 GB 全部导出）。
- 逆向结论：密钥 = `CRC32(归档名)` → SplitMix64 → 16 字节周期 XOR；
  条目数据先 XOR 解密再 zstd 解压。
- 自带 `manifest.json`：1783 条记录的完整清单，任何人都能独立复核。
